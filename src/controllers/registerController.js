const UserModel = require('../model/User');


const registerController = (req, res) => {
    const data = req.body;
        /*
            - cpf, name, email, password, university,
            city, state, birthday
        */

    // res.json({data: data});
}


module.exports = registerController;